<Window x:Class="WorkspaceLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Workspace Launcher"
        Icon="pack://application:,,,/launcher_icon.ico"
        Width="1200" Height="850"
        MinWidth="800" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="#0A0A0A"
        ResizeMode="CanResize"
        Closing="Window_Closing">

    <WindowChrome.WindowChrome>
        <WindowChrome 
            CaptionHeight="0" 
            GlassFrameThickness="1" 
            CornerRadius="0" 
            ResizeBorderThickness="8" 
            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Grid Background="#0A0A0A">
        <!-- The Margin="1" is CRITICAL: it prevents WebView2 from covering the small edge where Windows detects resize handles -->
        <wv2:WebView2 x:Name="webView" Margin="1" />
    </Grid>
</Window>
