<Window x:Class="WorkspaceLauncher.Core.CustomZoneEngine.UI.ZoneCanvasEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Editor de Zonas"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#01000000"
        WindowState="Normal"
        ShowInTaskbar="False">

    <Grid x:Name="RootGrid">

        <!-- WebView2: the React zone editor canvas (transparent bg set in code-behind) -->
        <wv2:WebView2 x:Name="webView" />

        <!-- Perimeter vignette overlay — subtle dark gradient inset that gives depth
             to the canvas editor without obscuring the editing area.
             Delivered by 4 thin gradient rectangles on each edge. -->
        <Rectangle IsHitTestVisible="False">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#55000000" Offset="0.0"/>
                    <GradientStop Color="#00000000" Offset="0.09"/>
                    <GradientStop Color="#00000000" Offset="0.91"/>
                    <GradientStop Color="#55000000" Offset="1.0"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>
        <Rectangle IsHitTestVisible="False">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#44000000" Offset="0.0"/>
                    <GradientStop Color="#00000000" Offset="0.07"/>
                    <GradientStop Color="#00000000" Offset="0.93"/>
                    <GradientStop Color="#44000000" Offset="1.0"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

    </Grid>
</Window>
